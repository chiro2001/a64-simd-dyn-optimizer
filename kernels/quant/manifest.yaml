# quant (256 elems) SVE2 manifest (docs/44).
kernel: quant
contract: upstream-exact
vl_bytes: 32
shape:
  kind: quant
  n: 256

candidate:
  symbol: dynopt_quant_256_sve2
  trace_driver_src: kernels/quant/trace_driver.cpp
  trace_driver_c: kernels/quant/trace_driver.cpp

reference:
  lib: build/x265-8-clang-sve/libx265.a
  symbol: quant_neon
  symbol_mangled: x265_quant_neon
  call: x265_quant_neon
  decl: |
    extern "C" uint32_t x265_quant_neon(
        const int16_t*, const int32_t*, int32_t*, int16_t*, int, int, int);

baselines:
  neon:
    driver: kernels/quant/trace_driver.cpp
    symbol_mangled: x265_quant_neon

corpus:
  strides: [1]
  value_range: [-32768, 32767]
  cases: 20000

layouts:
  compute: [sve2, sve1]
  widen: [unpk, smull-ones]

# Pure-NEON quant axes (--isa neon): nonzero count via u32 vaddvq
# accumulate (u8-mask vaddlvq variant was dropped: 20k diff failed on the
# cloud GCC's vaddlvq_u8 return handling, round-0023); shape full=32x
# unrolled 8-elem blocks, loop=single 8-elem body.
layouts_neon:
  count: [vaddv]
  shape: [full, loop]
