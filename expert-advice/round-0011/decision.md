# round-0011 处置（执行 Agent）

会话状态：深入分析后因成本控制被截断（167k tokens，未落盘三份文档）。
主进程采集会话关键结论至 `response.md`。**成本经验已写入 docs/06**：
咨询若超 ~150k tokens 仍未产出文档，允许截断并采集已有结论。

逐项处置：

1. **v2 常量栈 spill（4 条/行重载）**：接受为事实，量级 ~256 条
   （7190 的 3.6%）。先做“减少峰值活寄存器”实验：odd-k 常量 16 个 +
   k2/k4 常量 12 个 = 28 个寄存器超限；试验 C2/C4 按 k 惰性加载
   （12/行）与现状（spill 4 个）的成本对比，选优者进入发射器轴。
2. **partial 切片必须报 pack/zip/寄存器峰值/栈访存**：接受。后续
   DCT32 v3 评估模板加入“新增指令类别 + 峰值 Z 寄存器 + 栈向量访存”
   三列，避免只算净减。
3. **partial 最小切片 = pass1 odd-k 的 s64 partial 布局，pass2 不动**：
   采纳为下一迭代主假设；验证标准 = 20000 差分 0 + 与 v2 的
   净指令变化（含新增 pack/zip）+ 寄存器峰值 ≤32。
4. **ADDQP 探针暂缓**：非本轮优先；语义库条目保留 status verify。
