# round-0010 处置（执行 Agent）

会话状态：只读沙箱挡写（`apply_patch` 被拒），三份建议文档未能由模型落盘；
主进程从最终答复补录 `response.md`。**此后咨询改用 `-s workspace-write` +
prompt 目录约束（用户裁定 2026-08-13，已写入 docs/06 §5.2）。**

逐项处置：

1. **16x16 仅“接近减半”（189 > 186）**：接受。口径修正为“接近减半，
   实机 cycles 未测”；docs/19 已注明减半口径，补充 186 阈值表述。
2. **8x8 79 缺消融/下界证明**：接受。后续若继续 8x8，做逐轴消融
   （pack/sel、hi-tbl、归约）并量化下界；当前优先级低于 DCT32。
3. **guard 指向旧符号 + VL/ABI/双侧 guard/范围证明**：接受，排入
   工具健壮性清单（sa8d/sa8d16/dct32 的 guard 与 dispatch 复核）。
4. **16x32 跨 16x16 舍入不恒等**：事实成立（探针 4405/20000）。已删除
   未验证的 sa8d16x32 尝试；如重启，按“per_16x16 归约屏障”实现。
5. **实验顺序**：采纳“16x16 ≤186 → 8x8 消融”的 SA8D 顺序；但当前主线
   按用户指示为 DCT32（上游 12710 → 减半 6355 → 超越内部 4251/4827），
   该顺序在 DCT32 里程碑后执行。
6. **验证标准**：采纳（200k 零分歧、多 seed lite、guard、VL 拒绝、
   paired cycles）；DCT32/SA8D 的 finalist 按此执行。
7. **工具：fused_uop 半数硬门 + Pareto（关键路径/端口/访存/spill）**：
   采纳。DCT32 v2 = 7190（0.566x）尚未过半数门（6355），继续压；
   Pareto 维度加入后续发射器/评估迭代。
8. **ISA/DCT：p1 rounding-narrow/ADDQV 子集、p3 ADDQP 探针；SABAL 等
   暂缓**：接受，作为 ISA 目录补全的下一步；DCT“60 gap”口径已由
   DCT32 v2 数据取代（docs/20）。
