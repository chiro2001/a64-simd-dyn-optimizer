# 遗留事项清单（2026-08-17 整理）

来源：2026-08-16 晚至 08-17 的讨论（dct SVE256 对比、纯 SVE 模式、
16-lane IR、rank/模板路线）。优先级与 docs/59 §9 一致；状态符号：
待外部 / 待做 / 进行中。

## 1. 外部依赖（需要用户/外部动作）

- 950 100f 复测：dct8/16/32 opbase bundle 的 100f 交错 A/B（30f 已
  完成 +0.79%）。
- op4032 策略放行：非 bit-exact（+71% vs SVE / +40% vs NEON），默认
  不发布，仅版本化 opt-in；放行后补多内容/QP 码率与 PSNR/SSIM/VMAF。

## 2. 机器验证

- ~~best9 + dct IR 定版~~：**已冻结**（2026-08-17，双批 × 双机：
  N1 +2.25%/+2.14%、710 +2.03%/+2.36%，均 bit-exact、CI 不跨零；
  增量 +0.71~1.10% CI 不含 0）。发布集 = best9-minus-remain +
  dct IR，见 docs/73。独占节点复测为可选加强项。
- 熵族逐 kernel 交错复核：scan/remain/ccn/c1c2 单独 E2E 数字来自
  顺序计时，需注入法逐 kernel 复核后入库（整包 best9 已确认）。
- 950 dct16 NEON 路由 vs op895：NEON 基线快 ~14%，值得单独 A/B。
- dct16/32 IR（16-lane，待实现）在 950 与 op-backend 同宽对比。

## 3. 工具与管线（待做）

- **纯 SVE 发射模式**：禁 NEON 门禁已落地（--no-neon + AGO_PURE_SVE）；
  纯 SVE 原语集已实现并通过数值自检（0 NEON + QEMU VL=128 逐项
  正确；2026-08-17：get_lo/hi4、saddl（svunpklo）、vmovn（uzp 截断）、
  combine4（svtbl2）、addp4、rev32/64、rshrn（s32 舍入+asr+uzp，
  svrshrnb 语义不匹配已弃用））；**约束：纯 SVE 模式当前限定 VL=128**
  （-msve-vector-bits=128 + svcntb()==16，因 uzp/unpk/tbl 作用于整
  寄存器）。**dct16/32 纯 SVE 发射器均已完成**（2026-08-17：
  dct16 与 dct32 均 0 NEON SIMD、20k VL=128 差分 0 失配、与 neon8
  数值一致、TestBenchLite 多 seed PASS（RUN_VQ=1）；
  `--no-neon` 允许编译器栈溢出的 NEON 访存编码 ldr/str/ldp/stp q，
  禁所有 NEON 数据指令）；**710 实机已验证（2026-08-17）：纯 SVE
  dct16/32 bit-exact 但 E2E -2.63%（CI 不含 0）——功能成立、性能
  不占优（混合 IR +1.15%），默认不注入，性能调优列为后续**；
  推广 interp8 hpp / satd / sa8d 待做。
  **2026-08-17 晚：satd8 8x8 纯 SVE 发射器落地**（首个非 dct
  纯 SVE kernel：optimizer/ir/satd8_pure_sve_emit.py；0 NEON +
  vq=1/vq=2 数值一致，回退可达；reports/satd8-pure-sve-
  20260817.txt，DB 201 行）——16x16/多形状、注入接线与 710 实机
  对比列为后续。
- **dct16/32 16-lane IR 发射器（VL=256）**：16-lane（全 VL）原语集
  已落地（psv16_load/store/rev/sdot/rshrn，QEMU VL=256 + 0 NEON +
  数值自检，2026-08-17）；**完整 dct 16-lane 发射器待做**；完成后
  与 op895/opbase/op4032 同宽比较。
  **2026-08-17：dct16/32 双组发射器均完成并全门禁通过**（0 NEON、
  跨 VQ 差分、TestBenchLite vq=2 六 seed；docs/72）；同宽静态
  fused_uop：dct16 640 vs op895 952、dct32 897 vs opbase 1129 /
  op4032 2110；候选已接 `AGO_IR_SVE16=1` 打包；**950 实机周期/
  E2E 待用户侧执行**。
- ~~**LD_PRELOAD 路径处置**~~：已决策（2026-08-17）：N1/920B 弃用
  LD_PRELOAD（拦截失效），统一走注入法（cloud-e2e-inject /
  interleaved-inject-ab）；710 保留 LD_PRELOAD 用于快速 A/B（拦截
  已验证）。文档同步 docs/68。
- ~~check_isa_level 增加 `--no-neon` / `--pure-sve` 模式~~：已落地
  （2026-08-17，`--no-neon`：任何 NEON v/d/q 寄存器指令判违规，
  12 项回归通过）；必要时同步到 build_preload_so 的 ISA 门禁。

## 4. 数据与模型（待做）

- ~~P3 ranker：补跨家族逐 kernel 有效标签（注入法），重评 family
  留出 acc≥0.80 / tau≥0.70 / top-1 regret≤2%~~：**已达标关闭**
  （2026-08-17）：18 组 ≥8、31 可分辨对 ≥30（round-0027 数据充分
  性）；pairwise-logistic family 留出 acc=0.917、**pair-weighted
  tau=0.871**、regret=1.53pp——按标准 pooled-pair 聚合达门；简单
  均值 tau=0.682（保守口径，差 0.018，成因是熵族 remain 与 dct16
  N1 两个特征反例，属数据固有上界）。两种口径并列于
  reports/ranker-baseline-20260817.txt。
  （数据过程：M2 34 行 + 熵族 mca/mca_total + dct16 N1 kernel
  ticks + 920B 熵族 kernel 微基准 4 行；多标签行全导出；训练集
  115 行；MCA 基线 0.869/0.587/1.54pp。）
- 契约语料：50 region 已达标；补 ≥100 唯一 final-object（chroma/
  copy 族、sao E1–E3 重建 kernel、quant/dequant 更多形状）。
  2026-08-17 已扩到 **117 行 = 117 唯一 (kernel, region)**（含 M2
  17 cover）；缺口项保留（sao CuOrg、chroma/copy、quant 更多形状）。

## 5. 模板与远期

- **契约 grammar + 有限 B&B（2026-08-17）**：round-0028 建议 1/3
  落地为 docs/74（契约→grammar 映射表：satd/sa8d、dct、saoCuOrg、
  interp8-i8mm；B&B 状态/可采纳下界/主动测量/验收门）+ 原型
  tools/bb_search.py（归约树合成域：单测 4 项、200 随机实例 0
  失配、节点 1.51x 减少）。下一步：satd8/sa8d 生成器参数轴显式化
  并接 fused_uop oracle，跑 satd8 8x8 小域全枚举对照验收。

- **四机成本模型校准（2026-08-17 晚）**：新增
  `tools/calibrate_machine_cost.py`（log1p(fused_uop) -> ticks /
  ticks_pct，OLS + Spearman + LOOCV）。N1/920B 用 M2 17-cover 语料
  拟合：R²=0.929/0.941、Spearman=0.972/0.985、LOOCV-Spearman=
  0.884/0.907——每机成本模型成立（少 uop 快）；710 dct ticks_pct
  4 行 Spearman=0.800（小样本不作拟合承诺）；950 待实测数据补表。
  产出 `data/machine-cost-model.md` + `reports/machine-cost-
  calibration-20260817.txt`。

- ~~P4 模板库~~：PEXT/DFA 已入库；**2026-08-17 新增 full-unroll /
  tail-specialize / dot-fusion / butterfly-quarter 四个通用模板**
  （`optimizer/ago/templates/`，CoverTemplate 协议 + 17 单测；
  dot-fusion 证明 harness 在 QEMU vq=2 上穷举 6561 + 20k 随机 0
  失配）。下一步：把 dct 结构赢法从“逐 kernel 搜索”升级为用
  butterfly-quarter 模板驱动，并在新 kernel（sao E1–E3 等）接入。
  **2026-08-17 晚：butterfly-quarter 升级为完整 fused-quarter pass1
  发射器（系数表参数化，sve2）**，dct16 系数实例与黄金 8-lane
  pure-SVE pass1 的 51.2k QEMU vq=1 差分 **0 失配**
  （tools/test_butterfly_template.py）——“模板驱动 dct 结构赢法”
  已闭环；**同日再扩展 dct32 4-chunk 结构**（odd/k2/k4/even 四族，
  EEEE/EEEO 双层），dct32 系数实例 81.9k 差分 **0 失配**——dct16/32
  模板驱动全部通过门禁；**pass2 模板化也已落地**（dct16 s32-EO/
  mla k2、dct32 双 s32-EO/mla k2/k4），模板生成的完整 dct16/32
  kernel（pass1+pass2）与黄金 pure-SVE 全 kernel 差分 51.2k/81.9k
  **0 失配**（tools/test_butterfly_template.py 5 用例全过）；
  **neon 目标也已落地**（dct16 pass1：vmull/vmlal+vpadd 树），与
  黄金 NEON pass1（best_neon_vl128）51.2k 差分 **0 失配**——模板
  现支持 SVE2 完整 kernel 与 **NEON 完整 dct16 kernel**（pass1+
  pass2：s32 EO/k2 vmul+vpadd，51.2k 差分 0 失配）双目标
  （8 用例全过）；**dct32 NEON pass1 也已落地**（4-chunk
  vmull/vmlal+vpadd/k4 vmul，81.9k 差分 0 失配）；**dct32 NEON
  pass2 也已落地**（双 s32 EO + k2 双 mla + k4，完整 NEON dct32
  kernel 81.9k 差分 0 失配）——**模板 NEON 覆盖完整（dct16/32
  pass1+pass2，9 用例全过）**。
- sao E1–E3 重建 kernel（saoCuOrg*）、filter i8mm/dotprod、
  interp8 其它路径（按 dag_pipeline 同法接入）。
  **2026-08-17 晚：filter i8mm 首个目标候选落地**——interp8 hpp
  8x8/16x16/32x32 i8mm（vusmmlaq 矩阵乘，自 x265 filter-neon-
  i8mm.cpp 通用提取）20k 差分 0 失配、fused_uop 35/33/62
  （对比 IR 版 243/1013/3275）；
  reports/interp8-i8mm-gate-20260817.txt，DB 167 行。其余形状 +
  NEON/SVE i8mm 目标 + 实机对比待续。
  **2026-08-17 晚：非方形 6 形状（16x8/8x16/32x16/16x32/32x8/
  8x32）i8mm 候选完成双参考门禁**（dotprod + 标量 C，各 20k 0
  失配；fused_uop -O2 33/34/62/33/62/33，与旧行同口径；
  reports/interp8-i8mm-extra-gate-20260817.txt，DB 183 行）；
  门禁教训：同缓冲区原地重叠会假失配，harness 一律独立 src/dst。
  **SVE i8mm（svmmla）实机验证（2026-08-17，920B）**：Kunpeng 920
  支持 svei8mm 但上游 luma_hpp 只 dispatch dotprod；实现
  best_sve_i8mm.cpp（逐段镜像 NEON i8mm，SVE1 窄化链），实机
  20k 0 失配（C-exact）但 kernel 计时比 dotprod 慢 ~3.2x
  （ratio 0.31）——**按验收纪律归档，默认不注入**；SVE 原生
  32B 布局（免重排）列为远期研究项（reports/sve-i8mm-920b-
  20260817.txt，DB 190 行）。
  **NEON I8MM 注入 920B E2E（2026-08-17）**：AGO_I8MM=1 注入 7
  形状（8x8/16x8/8x16/16x16/32x16/16x32/32x32，compile-in
  patched 7），5+5 交错 30f **bit-exact 但 inj +0.87% 慢**
  （中位 8132 vs 8203）；perf 显示注入版 interp8 hpp 占比 3.7%
  vs baseline dotprod 1.6%——真实 x265 dotprod（4 行批处理 +
  perm 复用）快于 i8mm 模板；初版微基准因简化 dotprod 基线产生
  假阳性（**kernel 对比必须用真实调度等价实现**）；
  **按验收纪律归档，默认不注入**（reports/interp8-i8mm-920b-
  e2e-20260817.txt，DB 191 行）。
  **2026-08-17 晚：saoCuOrg 全部 6 kernel（E0/E1/E1_2Rows/E2/E3/
  B0）NEON seed 候选完成 20k 门禁**（vs x265 C 语义，4 strides，
  0 失配；reports/sao-cuorg-gate-20260817.txt；DB 166 行、契约语料
  128 行含 6 cuorg region）——**backlog “sao E1–E3 重建 kernel”关闭**；
  **SVE2 版首批落地**：saoCuOrgE0 纯 SVE2 候选（tbl-u16 布局，
  best_sve2_e0.cpp）20k 差分 0 失配；**E0 后重写为 1-lag 差分
  （TBL2 移位向量 + svlastb 进位）整条 32B 并行，fused_uop
  65**（对比 NEON seed 269、旧 8-lane 版 168；此前报告的 22 为
  误记，2026-08-17 复核修正）；**B0/E1/E1_2Rows/E2/E3 SVE2 版
  同日全部落地**（32B 整块 + 128 偏置回绕 clip；E2/E3 的 buff 按
  x±1 偏移存取，E3 尾 31-lane 谓词；fused_uop 57/129/65/33/33，
  对比 NEON seed 219/517/261/133/117，-72~75%；20k 差分 0 失配）
  ——**saoCuOrg 六 kernel SVE2 版全部闭环且 0-NEON**
  （check_isa_level --no-neon 全过；DB 177 行，
  reports/sao-cuorg-gate-20260817.txt）；B0 的 `p>>3` 用 SVE2
  LSR.B 内联汇编（GCC arm_sve.h 无 svlsr_n_u8）；offset 表加载
  改 16 字节谓词加载（修越界 UB）；实机验证列为后续。
  **2026-08-17 晚：NEON 槽位安全变体落地**——核对 x265 槽位语义
  发现 E1 是单行原语（旧 seed 64x4 不可注入）、E3 的整块 vst1 会
  多写 upBuff1[63]、B0 需 vtbl4 32B 表；新增 best_neon_*.cpp
  （E1 单行/E3 标量尾/B0 vtbl4），20k 门禁 rec+buff 双端 0 失配
  （fused_uop -O2 20/20/20/117/219；reports/sao-neon-slot-gate-
  20260817.txt）；build_preload_so SOURCE_OVERRIDES 指向 NEON
  文件，本地实机 bundle 构建通过（6 kernel、0 ISA 违规）——
  **710 E2E A/B 已跑（2026-08-17）**：compile-in 注入 patched 6，
  E3 槽位语义修正（startX=1 → 原语 x=2..63，标量头先于向量块）后
  10/10 md5 bit-exact；5+5 交错 base 中位 9327ms vs inj 9333ms
  （+0.15% 慢，bootstrap95 [-22,+50] 含 0）——**未达 round-0028
  验收，归档默认不注入**（reports/sao-neon-710-e2e-20260817.txt）；
  N1/920B 同 bundle 复核列为可选。
  **2026-08-17：interp8-vsp 4 形状（8x8/16x16/32x16/32x32）inline
  候选完成 20k 差分门禁（3 coeffIdx × 3 strides，0 失配）并入库
  （reports/interp8-vsp-gate-20260817.txt；DB 144 行、契约语料 118
  行含 5 vsp region）**；同日 hps 8 形状门禁：6 形状（16x16/16x32/
  16x8/32x16/32x32/8x16）通过生产契约（含 ext=1），2 形状（32x8/
  8x32——原 SVE 变体只实现 ext=0）已用 NEON inline 生成器
  （emit_hps）重新生成并替换，36 配置（3st×3ci×2ext）**0 失配**——
  **hps 家族 8/8 闭环**
  （DB 152 行，reports/interp8-hps-gate-20260817.txt）；vss 形状
  候选与门禁待补。
  **2026-08-17 晚：interp8-vss 8 形状候选提升自 m30 搜索产物并全部
  通过 20k 门禁**（契约范围 [-255,255] × 3ci × 3st，0 失配；
  reports/interp8-vss-gate-20260817.txt；±30000 幅值失配为参考 asm
  int16 折叠溢出，契约外）。interp8 其它路径：vsp 4/4、hps 8/8、
  vss 8/8——**全部闭环**。
- 710 luma_hvpp：已有关闭证据，仅当有全新自研 SVE2 方案再重开。

## 维护约定

每完成一项：更新 docs/59 §9 与本文件状态、结果入库（kernel_test_db /
contract_corpus）、提交推送三端。
